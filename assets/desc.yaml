name: desc_prof
channels:
  - bioconda
  - conda-forge
  - defaults

dependencies:
  # R 4.1.x → matches the Bioconductor builds below
  - r-base=4.1.*

  # GSL 2.6.x → provides libgsl.so.25
  - gsl=2.6.*

  # your other R deps
  - r-dplyr

  # Bioconductor‑mia (v1.0.2) built against R 4.1
  - bioconductor-mia=1.0.2

  # DirichletMultinomial v1.36.0 for R 4.1
  - bioconductor-dirichletmultinomial=1.34.0
